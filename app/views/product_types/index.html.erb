<h1>Lista de tipo de productos</h1>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Descripci&oacute;n</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

<% @product_types.each do |product_type| %>
  <tbody>
    <tr>
      <td><%= product_type.name %></td>
      <td><%= product_type.description %></td>
      <td><%= link_to 'Ver', product_type %></td>
      <td><%= link_to 'Editar', edit_product_type_path(product_type) %></td>
      <td><%= link_to 'Eliminar', product_type, method: :delete, data: { confirm: '¿Esta seguro?' } %></td>
    </tr>
  </tbody>
<% end %>
</table>

<br />

<%= link_to 'New tipo de producto', new_product_type_path, class: "btn btn-primary" %>
