<h1>Lista de roles</h1>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Descripci&oacute;n</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

<% @roles.each do |role| %>
  <tbody>
    <tr>
      <td><%= role.name %></td>
      <td><%= role.description %></td>
      <td><%= link_to 'Ver', role %></td>
      <td><%= link_to 'Editar', edit_role_path(role) %></td>
      <td><%= link_to 'Eliminar', role, method: :delete, data: { confirm: '¿Esta seguro?' } %></td>
    </tr>
  </tbody>
<% end %>
</table>

<br />

<%= link_to 'Nuevo rol', new_role_path, class: "btn btn-primary" %>
